#include <iostream>
#include <math.h>
#include "Unit1.h"

int main() {
	int a[n] = { 1, 5, -7, 8, 3, 5, -3, 0, -1, 3};
	int d, nd=0, b = 0, s = 0, k = 0;
	outmatr(a);
	min(a, d, nd);
	summa(a, b, s);
	sgatie(a, k);
}
